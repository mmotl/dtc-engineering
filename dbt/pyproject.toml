[project]
name = "dbt"
version = "0.1.0"
description = "DE Zoomcamp dbt module — local DuckDB analytics environment"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "dbt-duckdb>=1.7.0,<2.0.0",
    "duckdb>=1.0.0",
    "requests>=2.32.0",
]
